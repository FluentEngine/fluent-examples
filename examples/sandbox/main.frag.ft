#include "fluent.hlsl"

sampler(u_sampler, 2);
texture2D(float4, u_texture, 3);

struct VertexOutput
{
    float4 position : SV_Position;
    float3 normal : NORMAL0;
};

float4 main(VertexOutput stage_input)
{
    return float4(stage_input.normal, 1.0);
}
