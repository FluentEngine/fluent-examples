#include "fluent.hlsl"

sampler(u_sampler, 1);
texture2D(float4, u_texture, 2);

struct Input
{
    float3 color : TEXCOORD0;
};

float4 main(Input stage_input)
{
    return float4(stage_input.color, 1.0);
}
